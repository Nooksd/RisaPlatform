apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: gateway
  labels:
    app: gateway-api
spec:
  type: LoadBalancer
  selector:
    app: gateway-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http